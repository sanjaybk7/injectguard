{
  "name": "agentic-guard",
  "displayName": "agentic-guard",
  "description": "Static analyzer for prompt-injection and confused-deputy risks in LLM agent code.",
  "version": "0.1.0",
  "publisher": "sanjaybk7",
  "license": "Apache-2.0",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": ["Linters", "Other"],
  "keywords": ["security", "ai", "agents", "prompt-injection", "owasp", "langgraph"],
  "activationEvents": ["onLanguage:python"],
  "main": "./out/extension.js",
  "contributes": {
    "configuration": {
      "title": "agentic-guard",
      "properties": {
        "agentic-guard.cliPath": {
          "type": "string",
          "default": "agentic-guard",
          "description": "Path to the agentic-guard CLI executable. Defaults to looking it up on PATH."
        },
        "agentic-guard.runOn": {
          "type": "string",
          "enum": ["save", "manual"],
          "default": "save",
          "description": "When to run the scanner."
        }
      }
    },
    "commands": [
      {
        "command": "agentic-guard.scanWorkspace",
        "title": "agentic-guard: Scan workspace"
      },
      {
        "command": "agentic-guard.scanFile",
        "title": "agentic-guard: Scan current file"
      }
    ]
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "lint": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/vscode": "^1.85.0",
    "typescript": "^5.3.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/sanjaybk7/agentic-guard.git"
  }
}
